TASK: Implement point-of-no-return flash effect in Scene.tsx

When a body is absorbed (distance < playerRadius), create a brief white flash sprite at that world position. The flash should scale up (1→3) and fade out (opacity 1→0) over 150ms. Add constants: POINT_OF_NO_RETURN_FLASH_DURATION=0.15, POINT_OF_NO_RETURN_FLASH_COLOR=0xffffff

Also add a hasTriggeredNoReturnFlash flag to Body class to prevent repeat triggers.

keywords: emergence, subscriber, fault-tolerance, zooming interface, games
ULW

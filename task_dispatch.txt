TASK: Add upgrade max-level tracking + golden celebration effect

Idea #8: Golden glow + checkmark MASTERED effect for maxed upgrades in shop

Concept #12: Golden maxed effect: when upgrade reaches max level, add golden glow + brief particle burst. Shows player they have achieved the best version of that upgrade.

CONTEXT:
- Upgrade buttons in BlackHoleSimulator/src/components/UI.tsx (mass + speed upgrades)
- Player state tracked in BlackHoleSimulator/src/components/Scene.tsx
- Golden particle burst system already exists: createGoldenBurst() + goldenBurstsRef
- SaveManager in BlackHoleSimulator/src/systems/SaveManager.ts handles persistence

STEPS:
1. Read UI.tsx: find upgrade buttons (mass + speed), find how upgrades are triggered (onUpgradePurchase callback)

2. Read App.tsx: find how player state tracks upgrade levels (massLevel, speedLevel or similar)

3. Read SaveManager.ts: check if upgrade levels are persisted

4. Add max-level tracking:
   - Define MAX_UPGRADE_LEVEL = 5 for both mass and speed
   - Track current upgrade level for each in player state
   - Persist upgrade levels in SaveManager (add massLevel, speedLevel fields)
   - Load saved levels on game start

5. Add golden celebration when upgrade reaches max:
   - In Scene.tsx, check if upgrade purchase brings level to MAX_UPGRADE_LEVEL
   - If so, call createGoldenBurst() at player position
   - Set growthFlash = 1.0 on player for extra glow
   - Play success haptic: Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)

6. Add visual indicator in UI for maxed upgrades:
   - When massLevel or speedLevel === MAX_UPGRADE_LEVEL, add golden border/glow to that upgrade button
   - Show "MAX" badge or "★ MASTERED ★" text on the button

7. Run: cd BlackHoleSimulator && npx expo export --platform android 2>&1 | tail -10

OUTPUT: commit hash + brief description of changes
keywords: golden-glow,maxed-upgrade,celebration,mastered,upgrade-level
ULW
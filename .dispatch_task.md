# Dispatch #16 — Extract magic numbers to constants.ts

## Context
- Project: blsnkhole / BlackHole Simulator (Expo React Native + Three.js)
- Working dir: `/mnt/c/Users/enoma/Desktop/opencode-work/agent-works/game/blackhole-simulator`
- keywords: `flux`, `dismiss animation`, `mobile first`

## Task
Create `BlackHoleSimulator/src/systems/constants.ts` and extract all magic numbers from Scene.tsx and App.tsx into named constants.

## Rules (ULW)
- Do NOT use blanket find-replace — read each file and understand context first
- Preserve all existing functionality exactly
- Add brief comments explaining what each constant controls
- After creating constants.ts, update Scene.tsx and App.tsx to import and use them
- Do NOT change any logic, only replace hardcoded numbers with named constants
- Ensure the game still compiles without errors after changes
- Commit with conventional commit message

## Expected Output
1. `constants.ts` created with well-organised sections
2. `Scene.tsx` and `App.tsx` updated to import constants
3. Git commit + push

## constants to extract (from codebase analysis)
Group under clear headings:

**Physics**
- GRAVITY_CONSTANT
- PLAYER_BH_MASS
- ENEMY_BH_MASS_MIN / MAX
- SPAWN_DIST (distance at which objects spawn from player)

**Gameplay**
- LEVEL_THRESHOLDS (array of mass values for each level)
- WIN_MASS_THRESHOLD
- PLAYER_SPEED_BASE / SPEED_PER_UPGRADE
- ABSORBED_MASS_PER_LEVEL

**Rendering / Camera**
- ZOOM_MIN / ZOOM_MAX
- CAMERA_DIST_MIN / MAX
- BH_RENDER_MIN / MAX (size clamp for black hole visuals)

**Spawning**
- SPAWN_INTERVAL_BASE
- ENEMY_SPAWN_INTERVAL
- BODY_SPAWN_DIST_MIN / MAX

**Audio**
- BGM_FREQUENCIES (array)
- SFX_ABSORB_FREQ / SFX_LEVELUP_FREQ / SFX_DEATH_FREQ

**Difficulty**
- DIFFICULTY_EASY_MULT / NORMAL_MULT / HARD_MULT

(If any of these don't exist in the code, skip them — only extract numbers that actually exist as magic literals)

---
⚡ Focus on quality over quantity. Extract what exists, skip what doesn't.
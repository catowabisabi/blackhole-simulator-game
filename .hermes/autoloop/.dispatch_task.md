Fix ember particle burst on body absorption.

**Task**: Implement an EmberSystem that spawns glowing particle embers when the player BH absorbs a body.

**Files to modify**: `BlackHoleSimulator/src/components/Scene.tsx`

**Requirements**:

1. **EmberSystem class** (add after TrailSystem):
   - Pool: max 100 ember particles
   - Each ember: { position: Vector3, velocity: Vector3, life: number, maxLife: number, size: number, color: Color }
   - `spawn(position, mass)`: spawn 3-5 embers for small bodies, 10-20 for large (mass > 50 = large). Random velocity 0.5-2 units/s outward. Life 3-8s random. Size 0.5-1.5. Color: orange-yellow for small, white-orange for large.
   - `update(delta)`: move embers, decrement life, remove dead
   - `render()`: render as small glowing spheres (additive blending)

2. **emberRef**: add `emberRef = useRef(new EmberSystem())` after trailRef in Scene component initialization.

3. **Call on absorption**: In the absorption block (~line 1504), call:
   ```
   const absorbedPos = b.mesh.position.clone();
   emberRef.current.spawn(absorbedPos, b.mass);
   ```

4. **Update loop**: In the animation frame loop, call `emberRef.current.update(delta)` and `emberRef.current.render()` after `trailRef.current.render()`.

5. **Import Three.js**: Ensure `import * as THREE from 'three'` is at the top.

**Key constraint**: Follow existing particle/trail patterns in the file. Do NOT rewrite unrelated code. Just add the ember system and call it.

**Prompt keywords (for creativity)**: 平面图 · backpropagation · layering
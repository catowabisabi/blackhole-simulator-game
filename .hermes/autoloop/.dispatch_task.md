Fix streak combo UI wiring in App.tsx + UI.tsx.

**Task**: Wire the streak system from Scene.tsx → App.tsx → UI.tsx so the player can SEE their combo streak during gameplay.

**Files to modify**:
1. `BlackHoleSimulator/App.tsx` — add state + handler + pass prop
2. `BlackHoleSimulator/src/components/UI.tsx` — add streak display

**Requirements**:

### App.tsx
- Add `streakCount` (number) and `streakMultiplier` (number) state (init: 0, 1.0)
- Add `handleStreakChange(count: number, multiplier: number)` callback that updates these states
- Pass `onStreakChange={handleStreakChange}` to the `<Scene />` component
- Pass `streakCount` and `streakMultiplier` to `<UI />`
- In the stats Alert.alert (lines ~250-254), add `最高連擊 Max Streak: ${SaveManager.getMaxStreak()}` to the stats string

### UI.tsx
- Add `streakCount?: number` and `streakMultiplier?: number` to UIProps
- Add `streakCount = 0` and `streakMultiplier = 1.0` to destructured props
- In the JSX (top-right area near the score chip), add conditional rendering:
  ```
  {streakCount >= 2 && (
    <View style={streakStyles.container}>
      <Text style={streakStyles.multiplier}>×{streakMultiplier.toFixed(1)}</Text>
      <Text style={streakStyles.counter}>COMBO ×{streakCount}</Text>
    </View>
  )}
  ```
- Add `streakStyles` StyleSheet with: container (position:absolute, top:55, right:12, alignItems:'flex-end'), multiplier (fontSize:16, fontWeight:'bold', color:'#FFD700'), counter (fontSize:13, color:'#FFF', textShadow)

**Key constraint**: Follow existing patterns in the file (StyleSheet patterns, component structure). Do NOT rewrite unrelated code. Just add the minimal wiring.

**Prompt keywords (for creativity)**: kinesthetic · brushstroke · parallel-safe
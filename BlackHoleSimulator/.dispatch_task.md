# Dispatch #18 — Implement pause button + overlay

## Context
- Project: blsnkhole / BlackHole Simulator (Expo React Native + Three.js)
- Working dir: /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/game/blackhole-simulator/BlackHoleSimulator
- keywords: pause, overlay, game state, UI button

## Task
Pause state is defined in the game interface but no button or overlay renders it. The `gameState` includes 'paused' but there's no way to enter this state and no UI for it.

Find the game interface/types to confirm the `paused` state exists. Add:
1. A pause button (⏸) in the HUD - top-right corner, visible during gameplay
2. When pressed: set gameState to 'paused', show a semi-transparent dark overlay with "PAUSED" text and a resume button
3. Resume button: dismiss overlay, set gameState back to 'playing'
4. The pause button should only appear when gameState is 'playing'

Look at existing HUD elements in App.tsx for placement/styling patterns.

## Rules (ULW)
- Do NOT change any gameplay mechanics
- Only add pause UI - button and overlay
- Preserve all existing functionality
- After implementing, verify the code still compiles
- Commit with conventional commit message

## Expected Output
1. Pause button added to HUD (visible during 'playing' state)
2. Pause overlay with "PAUSED" text + resume button
3. Git commit + push
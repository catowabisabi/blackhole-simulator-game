# TASK #20 — Event Horizon Memory: Ghost Echo After Death

## Context
In `BlackHoleSimulator/src/components/Scene.tsx`, refs `ghostEchoRef` and `ghostFadeRef` are declared (lines 691-692) and `createGlowTexture` helper exists (line 464), but no actual ghost-echo logic is implemented. The player death occurs at line ~1065 (`gameStateRef.current = 'lost'`).

## Your Task

**Implement the ghost echo feature:**

1. **On player death** (`gameState` transitions to `'lost'`), create a ghost black hole at the player's final position:
   - Clone the player mesh geometry + material (semi-transparent, same size as player's largest BH moment)
   - Add a glow sprite using `createGlowTexture(0x333333, 0.5)` 
   - Store in `ghostEchoRef.current = { mesh, glow }` and reset `ghostFadeRef.current = 1.0`

2. **In the animate loop**, update the ghost if it exists:
   - Decrease `ghostFadeRef.current` by `dt * 0.1` (10 second fade)
   - Set `mesh.material.opacity = ghostFadeRef.current`
   - Set `glow.material.opacity = ghostFadeRef.current * 0.5`
   - When `ghostFadeRef.current <= 0`: remove ghost from scene, set `ghostEchoRef.current = null`

3. **On game restart** (when `gameState` goes `'idle'` → `'playing'`), clean up any ghost immediately.

## Constraints
- Keep `createGlowTexture` — it's already there and correct
- Keep `ghostEchoRef` and `ghostFadeRef` declarations — already exist
- Do NOT change any other code
- Do NOT add new imports unless absolutely necessary

## Verification
After implementing, grep for `ghostEcho` to confirm: (a) creation on death, (b) animate update, (c) cleanup on restart. ULW
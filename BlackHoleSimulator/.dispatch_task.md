# Dispatch #17 — Validate spawn distance (object/BH overlap prevention)

## Context
- Project: blsnkhole / BlackHole Simulator (Expo React Native + Three.js)
- Working dir: /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/game/blackhole-simulator
- keywords: overlap, collision prevention, spawn safety

## Task
Objects (Planet, Star, Comet) currently spawn at random camera-edge positions without checking distance from AI black holes. This can cause objects to spawn inside enemy BHs.

Find the object spawn logic in BlackHoleSimulator/src/components/Scene.tsx (likely inside createBodies() or similar). Add a validation loop: before placing a new object, iterate through all AI black holes and ensure the spawn position is at least (enemyBH.radius + bodyRadius + 50) units away from every AI BH.

Use constants from constants.ts where applicable (e.g., SAFETY_MARGIN).

## Rules (ULW)
- Do NOT change any gameplay mechanics
- Only add the distance validation - do not touch spawn position randomization logic
- Use existing constants where possible, add new ones to constants.ts if needed
- Preserve all existing functionality
- After implementing, verify the code still compiles
- Commit with conventional commit message

## Expected Output
1. Updated spawn logic in Scene.tsx
2. New constants if needed in constants.ts
3. Git commit + push